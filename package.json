{
  "devDependencies": {
    "@biomejs/biome": "2.4.6"
  },
  "scripts": {
    "api": "pnpm --filter api",
    "build": "sudo rm -rf ./data ./postgres_data && docker compose down -v && docker compose up --build ",
    "format": "biome check --write",
    "start": "docker compose up"
  }
}
